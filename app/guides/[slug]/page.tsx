import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getGuide,guides } from "@/lib/guides";
import { getReply } from "@/lib/data";
export function generateStaticParams(){return guides.map(g=>({slug:g.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const g=getGuide(slug);if(!g)return{};return{title:g.title,description:g.description,alternates:{canonical:`/guides/${g.slug}`},openGraph:{title:g.title,description:g.description,type:"article",url:`/guides/${g.slug}`}}}
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=getGuide(slug);if(!g)notFound();const related=g.relatedSlugs.map(getReply).filter(Boolean);const schema={"@context":"https://schema.org","@type":"Article",headline:g.title,description:g.description,author:{"@type":"Organization",name:"CopyReply"},publisher:{"@type":"Organization",name:"CopyReply"}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="pagehero"><div className="container"><div className="breadcrumb"><Link href="/">Home</Link> / <Link href="/guides">Guides</Link> / {g.title}</div><span className="pill">{g.category}</span><h1>{g.title}</h1><p>{g.description}</p></div></section><article className="container article-page"><p className="article-intro">{g.intro}</p>{g.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2><p>{s.body}</p></section>)}<section className="guidecard"><h2>Quick reply checklist</h2><ol><li>Acknowledge the message or situation.</li><li>State your answer or purpose clearly.</li><li>Include only relevant context.</li><li>End with a useful next step.</li><li>Check names, dates, facts, and tone.</li></ol></section>{related.length>0&&<section><h2>Ready-to-copy examples</h2><div className="replygrid">{related.map(r=><Link className="replycard" href={`/reply/${r!.slug}`} key={r!.slug}><h3>{r!.title}</h3><p>{r!.description}</p><span className="arrow">View replies →</span></Link>)}</div></section>}</article></>}
