import Link from "next/link";import {arabicGuides} from "@/lib/arabicData";export default function Page(){return <main className="section"><div className="container"><div className="sectionhead"><div><h1>أدلة التواصل المهني</h1><p>نصائح عملية تساعدك على كتابة رسائل أوضح وأكثر تأثيراً.</p></div></div><div className="linkgrid">{arabicGuides.map(([title,slug,description])=><Link className="card" href={`/ar/guides/${slug}`} key={slug}><h2>{title}</h2><p>{description}</p></Link>)}</div></div></main>}
