import type { Metadata } from "next";import Link from "next/link";import {notFound} from "next/navigation";import {arabicCategories,arabicReplies} from "@/lib/arabicData";import {site} from "@/lib/site";
export function generateStaticParams(){return arabicCategories.map(c=>({slug:c.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const c=arabicCategories.find(x=>x.slug===slug);return c?{title:c.name,description:c.description,alternates:{canonical:`${site.url}/ar/category/${c.slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=arabicCategories.find(x=>x.slug===slug);if(!c)notFound();const items=arabicReplies.filter(r=>r.categorySlug===slug);return <main className="section"><div className="container"><div className="breadcrumb"><Link href="/ar">الرئيسية</Link> / {c.name}</div><div className="sectionhead"><div><h1>{c.name}</h1><p>{c.description}</p></div></div><div className="linkgrid">{items.map(r=><Link className="card" href={`/ar/reply/${r.slug}`} key={r.slug}><h2>{r.title}</h2><p>{r.description}</p><span className="textlink">عرض الردود ←</span></Link>)}</div></div></main>}
