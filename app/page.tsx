import Link from "next/link";
import { Briefcase, Headphones, Heart, MessageCircle, GraduationCap, CalendarCheck, Sparkles, Handshake, Users, Plane, Stethoscope } from "lucide-react";
import SearchForm from "@/components/SearchForm";
import HomeDiscovery from "@/components/HomeDiscovery";
import { categories,replies } from "@/lib/data";
const icons:any={Briefcase,Headphones,Heart,MessageCircle,GraduationCap,CalendarCheck,Handshake,Users,Plane,Stethoscope};
export default function Home(){return <><section className="hero"><div className="container"><span className="eyebrow"><Sparkles size={15}/> 120 useful replies, one search away</span><h1>Find the <span className="gradient">perfect reply</span> in seconds.</h1><p className="lead">Professional, friendly, polite, and confident replies for work, customers, social messages, invitations, and everyday conversations.</p><SearchForm/><div className="heroactions"><Link className="button" href="/studio">Open Reply Studio</Link></div><div className="chips"><span className="chip">Recruiters</span><span className="chip">Customers</span><span className="chip">Thank you</span><span className="chip">Birthday wishes</span><span className="chip">Invitations</span></div></div></section><section className="section" id="categories"><div className="container"><div className="sectionhead"><div><h2>Browse by category</h2><p>Start with the situation that best matches your message.</p></div></div><div className="grid">{categories.map(c=>{const Icon=icons[c.icon];return <Link className="card" href={`/category/${c.slug}`} key={c.slug}><div className="iconbox"><Icon size={22}/></div><h3>{c.name}</h3><p>{c.description}</p></Link>})}</div></div></section><HomeDiscovery replies={replies} /></>}
