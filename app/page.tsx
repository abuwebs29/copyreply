import Link from "next/link";
import { Briefcase, Headphones, Heart, MessageCircle, GraduationCap, CalendarCheck, Sparkles } from "lucide-react";
import SearchForm from "@/components/SearchForm";
import { categories,replies } from "@/lib/data";
const icons:any={Briefcase,Headphones,Heart,MessageCircle,GraduationCap,CalendarCheck};
export default function Home(){return <><section className="hero"><div className="container"><span className="eyebrow"><Sparkles size={15}/> Thousands of useful replies, one search away</span><h1>Find the <span className="gradient">perfect reply</span> in seconds.</h1><p className="lead">Professional, friendly, polite, and confident replies for work, customers, social messages, invitations, and everyday conversations.</p><SearchForm/><div className="chips"><span className="chip">Recruiters</span><span className="chip">Customers</span><span className="chip">Thank you</span><span className="chip">Birthday wishes</span><span className="chip">Invitations</span></div></div></section><section className="section" id="categories"><div className="container"><div className="sectionhead"><div><h2>Browse by category</h2><p>Start with the situation that best matches your message.</p></div></div><div className="grid">{categories.map(c=>{const Icon=icons[c.icon];return <Link className="card" href={`/category/${c.slug}`} key={c.slug}><div className="iconbox"><Icon size={22}/></div><h3>{c.name}</h3><p>{c.description}</p></Link>})}</div></div></section><section className="section"><div className="container"><div className="sectionhead"><div><h2>Popular replies</h2><p>Ready-to-copy wording for common situations.</p></div><Link className="arrow" href="/search">View all →</Link></div><div className="replygrid">{replies.slice(0,6).map(r=><Link className="replycard" href={`/reply/${r.slug}`} key={r.slug}><span className="pill">{r.category}</span><h3>{r.title}</h3><p>{r.description}</p><span className="arrow">View replies →</span></Link>)}</div></div></section></>}
