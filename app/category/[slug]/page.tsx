import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { categories,getCategory,replies } from "@/lib/data";
import { guides } from "@/lib/guides";
export function generateStaticParams(){return categories.map(c=>({slug:c.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const c=getCategory(slug);if(!c)return{};return{title:`${c.name} Reply Templates`,description:`${c.description} Browse ready-to-copy examples and practical communication guidance.`,alternates:{canonical:`/category/${c.slug}`}}}
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=getCategory(slug);if(!c)notFound();const items=replies.filter(r=>r.categorySlug===slug);const categoryGuides=guides.filter(g=>g.categorySlug===slug);const faq=[{q:`What are ${c.name.toLowerCase()} reply templates?`,a:`They are editable message examples designed for common ${c.name.toLowerCase()} situations. Choose the closest example and personalize names, dates, facts, and next steps.`},{q:"Can I use these templates exactly as written?",a:"Yes, but adding one specific detail makes the message more natural and accurate."},{q:"Which tone should I choose?",a:"Use professional wording for formal or unfamiliar recipients, and warmer wording when the relationship allows it."}];const schema={"@context":"https://schema.org","@graph":[{"@type":"CollectionPage",name:`${c.name} Reply Templates`,description:c.description,url:`https://copyreply.com/category/${c.slug}`},{"@type":"FAQPage",mainEntity:faq.map(x=>({"@type":"Question",name:x.q,acceptedAnswer:{"@type":"Answer",text:x.a}}))}]};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="pagehero"><div className="container"><div className="breadcrumb"><Link href="/">Home</Link> / {c.name}</div><span className="pill">{items.length} situations</span><h1>{c.name} reply templates</h1><p>{c.description} Choose a situation, copy a suitable reply, and adapt it before sending.</p></div></section><main className="container category-page"><section className="guidecard"><h2>How to get the best result</h2><div className="checkgrid"><div><strong>Choose the closest situation</strong><span>Start with the page that matches the purpose of your message.</span></div><div><strong>Match the relationship</strong><span>Use a more formal tone for unfamiliar or senior recipients.</span></div><div><strong>Add concrete details</strong><span>Include the correct name, date, decision, amount, or next step.</span></div><div><strong>Review before sending</strong><span>Check facts and remove anything that does not sound like you.</span></div></div></section><section><div className="sectionhead"><div><h2>Browse {c.name.toLowerCase()} replies</h2><p>Ready-to-copy options for common situations.</p></div></div>{items.length?<div className="replygrid">{items.map(r=><Link className="replycard" href={`/reply/${r.slug}`} key={r.slug}><span className="pill">{r.category}</span><h3>{r.title}</h3><p>{r.description}</p><span className="arrow">View replies →</span></Link>)}</div>:<div className="empty">More replies are being prepared for this category.</div>}</section>{categoryGuides.length>0&&<section><div className="sectionhead"><div><h2>Related guides</h2><p>Learn the principles behind better messages.</p></div></div><div className="replygrid">{categoryGuides.map(g=><Link className="replycard" href={`/guides/${g.slug}`} key={g.slug}><span className="pill">Guide</span><h3>{g.title}</h3><p>{g.description}</p><span className="arrow">Read guide →</span></Link>)}</div></section>}<section className="faqsection"><div className="sectionhead"><div><h2>Frequently asked questions</h2></div></div><div className="faqlist">{faq.map(x=><details key={x.q}><summary>{x.q}</summary><p>{x.a}</p></details>)}</div></section></main></>}
